# Olive Studios — website

Static single-page site (React + Babel via CDN, no build step), served by GitHub Pages. Entry: `index.html`.

Forms route to The Collector's Circle (Google Apps Script → Google Sheet). Endpoint in `src/collector-config.js`. Backend: https://github.com/Marwanemad123/the-collectors-circle
